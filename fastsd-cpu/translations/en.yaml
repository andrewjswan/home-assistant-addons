configuration:
  ui_mode:
    name: UI Mode
    description: Run FastSD CPU in Web UI mode or API mode
  cache:
    name: External cache
    description: Use external cache in share folder
  gpu:
    name: GPU
    description: Set GPU as Device
  log_level:
    name: Log Level
